/*function hideElement(targetElement, classToRemove) {    $(targetElement).removeClass(classToRemove);}$(document).ready(function(){    $('.mobile-menu').click(function (e) {        var target = e.target.nodeName;        e.stopPropagation();        e.preventDefault();        if (($(target).hasClass('mobile-menu') || $(target).parent().hasClass('mobile-menu')) && !$('#navbar').hasClass('show-on-mobile')) {            $('#navbar').addClass('show-on-mobile');            $(this).addClass('current-selection');            return false;        }        if (!$(target).hasClass('nav-ul') && $('#navbar').hasClass('show-on-mobile')) {            var targetToHide = '.nav-ul';            hideElement(targetToHide, 'show-on-mobile');        }    });    $(document).click( function(){        $('#navbar').removeClass('show-on-mobile');    });});*//* TOP/MAIN NAVBAR MOBILE TOGGLE ON/OFF */$(document).ready(function(){    $('.show-nav').click(function(mn){        if ($(this).parent().hasClass('show')){            $(this).parent().removeClass('show');        }        else {            $(this).parent().addClass('show');        }    });    $('.nav li').click(function(msn){        if ($(this).hasClass('show')||$(this).parent().hasClass('show')) {            $(this).removeClass('show');        }        else {            $(this).addClass('show');        }    });});/* Bottom Navbar sticks to top when page scrolled */$(document).ready(function(){    function stickyNav() {        var $topBar = $('.top-bar');        if ($topBar.length > 0) {            var offsetTop = $topBar.offset().top;            $(window).scroll(function () {                var scrollOffset = $(this).scrollTop();                if (scrollOffset >= offsetTop) {                    $topBar.addClass('sticky');                } else {                    $topBar.removeClass('sticky');                }            });        }    }    stickyNav();    $(window).resize(function(){        stickyNav();    });});